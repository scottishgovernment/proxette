description "MyGov Authenticating Proxy"
author "mygov.scot team"

start on runlevel [2345]
stop on runlevel [!2345]

setuid proxette
chdir /opt/proxette

script
    . /etc/profile
    exec node src/run.js >> /var/log/proxette/proxette.log 2>&1
end script
